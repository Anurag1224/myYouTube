const Button = ({name}) => {
    return (
        <div className="">
            <button className="px-2 py-[2px] mx-1 my-2 bg-gray-200 rounded-md font-medium" >{name}</button>
        </div>
    );
};

export default Button;
